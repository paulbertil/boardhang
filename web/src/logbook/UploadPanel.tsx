// Placeholder — the real uploader (auth gate, file chooser, consent, manage-own list) is
// built in U4. Kept minimal so the tab shell (U3) builds and renders.

export function UploadPanel() {
  return (
    <div className="rounded-lg border border-dashed border-border p-6 text-center text-sm text-muted-foreground">
      Uploading your MoonBoard file — coming here shortly.
    </div>
  )
}
